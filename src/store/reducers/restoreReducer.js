import { SET_ALL_COINS, SET_NAVIGATION, SET_PORTFOLIO_COINS } from '../type'
const initialState = {}

export const restoreReducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state
	}
}
